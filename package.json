{
  "name": "qa-superpower-enterprise",
  "version": "4.0.0",
  "private": true,
  "description": "Distributed enterprise QA engineering platform with AI, Kubernetes workers, SSO, test-management sync, Git storage, visual regression, live WebSockets, contract/mobile/accessibility testing and autonomous regression planning",
  "engines": {"node": ">=22.0.0"},
  "scripts": {
    "start": "node server.js",
    "worker": "node worker.js",
    "check": "node --check server.js && node --check worker.js && node scripts/check-modules.js && node scripts/check-frontend.js",
    "test": "node scripts/selftest-v4.mjs"
  },
  "dependencies": {
    "playwright": "^1.63.0",
    "@playwright/test": "^1.63.0",
    "ldapts": "^9.0.0"
  }
}
